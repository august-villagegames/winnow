---
name: winnow
description: Research a broad candidate set and publish a self-contained anonymous Winnow comparison session.
---

# Winnow portable skill

Read `references/protocol.md` and use the same workflow as the repository-local
`.agents/skills/winnow/SKILL.md`: research 12–24 candidates across 6–10
factors, validate a complete seed, compile the committed runtime, and publish
one new anonymous here.now Site with `python3 scripts/winnow.py publish
seed.json`. Never read credentials, send `Authorization`, claim or update a
Site, expose a claim token, embed source HTML/scripts, or silently interpret
free text. A continuation package must always produce a separate successor
seed and URL.
